## Event Driven Architechture
- this refers to the async, event driven arch, in which certain kinds of objects (called emitters), emit named events and cause function objects ("listners") to be called.

-- so what we are going to build is
- something tag triggers a listner, to do some stuff incase of a emmitter emit.
